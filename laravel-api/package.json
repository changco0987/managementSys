{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "artisan:update": "php artisan cache:clear && php artisan config:clear && php artisan route:clear && php artisan view:clear && php artisan clear-compiled && php artisan config:cache && php artisan route:cache"
    },
    "devDependencies": {
        "axios": "^1.7.4",
        "concurrently": "^9.0.1"
    }
}
